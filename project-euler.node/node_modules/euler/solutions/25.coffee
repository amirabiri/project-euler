BigNumber = require 'bignumber.js'

exports.solve = ->
  a   = BigNumber(1)
  b   = BigNumber(1)
  ord = 2

  while true
    c = a.plus(b)
    a = b
    b = c
    ord++
    if c.toFixed(0).length == 1000
      break

  return ord
