BigNumber = require 'bignumber.js'

exports.solve = ->
  twoPow1000 = new BigNumber(2).toPower(1000).toFixed(0)
  sum = 0
  for i in [0..twoPow1000.length - 1]
    sum += twoPow1000.charCodeAt(i) - 48
  return sum
