fs = require 'fs'

exports.matrix = (filename) ->
  lines = fs.readFileSync(filename).toString().split(/\r?\n/)
  matrix = []
  for line in lines
    matrix.push line.split(/\s+/).map((num) -> parseInt num)
  matrix;
