BigNumber = require 'bignumber.js'

exports.solve = ->
  num = BigNumber(1)

  for i in [2..100]
    num = num.times(i)

  sum = 0
  numStr = num.toFixed(0)
  for chr in numStr
    sum += parseInt(chr)

  return sum
